{
  "version": 2,
  "builds": [{ "src": "package.json", "use": "vercel-sapper" }],
  "routes": [
    {
      "src": "/auth/(.*)",
      "dest": "/auth/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
